# Suggested Commands

- Current repository is documentation/prototype-only; no project-specific dev, build, lint, typecheck, test, or deploy command is available.
- Safe status/quality checks for documentation changes: `git diff --check`, `git status --short`.
- When implementation scaffolding is added, replace this memory with commands sourced from the committed package manifest and configuration.