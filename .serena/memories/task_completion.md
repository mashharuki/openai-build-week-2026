# Task Completion

- Documentation/steering work: run `git diff --check` and inspect `git status --short`; preserve unrelated user changes.
- There are no application validation commands yet. Do not claim builds, tests, typechecks, linting, or deployment until the corresponding committed tooling exists.
- When code is introduced, update this memory with exact manifest-backed lint, format, typecheck, test, and build commands.